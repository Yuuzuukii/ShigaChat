# ShigaChat — プロダクト紹介

## プロダクト概要

**ShigaChat** は、滋賀県在住の外国人住民を対象にした多言語 AI Q&A チャットシステムです。
滋賀県国際協会（SIA）が公開している生活相談 Q&A データベースを知識源として、ユーザーの質問に対して AI が自然言語で回答を生成・ストリーミング配信します。

- 対応言語：日本語・英語・中国語・ベトナム語・韓国語・ポルトガル語・スペイン語・タガログ語・インドネシア語（9言語）
- 想定ユーザー：滋賀県在住の外国人住民、および支援スタッフ

---

## プロダクトの意義・目的

### 背景にある課題

日本に暮らす外国人住民は、行政手続き・医療・子育てなど生活上の様々な場面で情報にアクセスする際に言語の壁に直面します。滋賀県国際協会は多言語の相談 Q&A を Web 上で公開していますが、

- **検索性が低い**：キーワード検索に頼るため、聞きたいことをうまく言語化できないと答えにたどり着けない
- **言語対応が不完全**：全ての Q&A が全言語に翻訳されているわけではない
- **対話的でない**：一問一答の静的なコンテンツであり、文脈に応じた補足ができない

### ShigaChat が解決すること

| 課題 | ShigaChat の解決策 |
|---|---|
| 検索性の低さ | ベクトル類似検索（RAG）により、自然文の質問でも関連 Q&A を高精度に取得 |
| 言語の壁 | 9言語対応の UI と、質問・回答の自動翻訳・要約・平易化アクション |
| 静的コンテンツ | スレッド型チャット UI により文脈を保ちながら会話的にやり取り可能 |
| 情報の鮮度 | SIA の Q&A をスクレイピング・再インジェクションするメンテナンス機能で知識源を最新に保つ |

---

## システムアーキテクチャ

```
[React フロントエンド]
        ↕ REST / SSE
[FastAPI メインサービス]  ←→  [PostgreSQL + pgvector]
        ↕ HTTP
[FastAPI エージェントサービス (LangGraph)]
        ↕
[OpenAI API (GPT / Embeddings)]
```

### サービス分割の設計意図

メインサービスとエージェントサービスをマイクロサービスとして分離することで、

- エージェントの複雑な処理フロー（LangGraph）をメインサービスのビジネスロジックから独立させる
- エージェントの差し替え・スケールアウトをサービス単位で行える

---

## 技術スタックと選定理由

### フロントエンド：React + Tailwind CSS

- **React**：コンポーネント単位での状態管理と再利用性を重視。`UserContext` による認証状態のグローバル管理や SSE によるストリーミング受信など、動的 UI の実装に適している
- **Tailwind CSS**：ユーティリティファーストにより、多言語対応で文字幅が変動する UI を素早くプロトタイプ・調整できる

### バックエンド：FastAPI（Python）

- **FastAPI**：非同期処理に優れ、SSE（Server-Sent Events）によるトークンのストリーミング配信との相性が良い。Pydantic による型安全なスキーマ定義も生産性に直結する
- **DDD（ドメイン駆動設計）**：`controllers / usecases / domain / repositories / infrastructure` の5層構造を採用。ビジネスロジックをフレームワークや DB から切り離し、テスト容易性と保守性を高めた

### エージェント：LangGraph

- **LangGraph**：RAG パイプラインを「ノードとエッジのグラフ」として宣言的に記述できるため、処理フローの分岐（参照あり・なし回答、ルーティング）が可視化しやすい。LangGraph Studio によるデバッグも有効活用した
- 処理ステップ（言語検出 → クエリ書き換え → ベクトル検索 → 参照選定 → 回答生成）ごとに進捗イベントを SSE 配信し、ユーザー体験を改善

### ベクトルデータベース：PostgreSQL + pgvector

- 既に PostgreSQL を RDB として採用していたため、**別途ベクトル DB を追加導入するコストを省きつつ**ベクトル類似検索を実現できる pgvector を選択
- SIA の Q&A をあらかじめ OpenAI Embeddings でベクトル化して保存し、質問時にコサイン類似度で関連文書を取得する RAG を構築

### インフラ：Docker Compose + Nginx

- `web / service / agent / postgres / maintenance` の各サービスを Docker Compose で一括管理。ローカル開発から本番環境まで同一構成で再現できる
- Nginx をリバースプロキシとして配置し、フロントエンドの静的配信とバックエンド API へのルーティングを一元化

---

## 主要機能

### 1. RAG による回答生成

1. ユーザーの質問を OpenAI Embeddings でベクトル化
2. pgvector でコサイン類似検索 → 関連 Q&A を取得
3. 参照品質を LLM でフィルタリング
4. 参照あり/なしを動的に切り替えてプロンプト構築
5. GPT による回答をトークン単位でストリーミング配信

### 2. 多言語対応

- UI の全テキストを9言語で i18n 管理
- `langid` による質問の言語自動検出
- 回答後に「翻訳」「要約」「平易化」のアクションを提供

### 3. スレッド型チャット

- 会話履歴をスレッドとして永続化
- 文脈を考慮したクエリ書き換えにより、前の発言を参照した質問にも対応

### 4. 知識源の自動更新

- SIA の Web サイトをスクレイピングして Q&A を取得・差分更新
- Embeddings の再生成・再インジェクションをスクリプト1行で実行

---

## 課題と取り組み

### 課題1：回答品質の安定性

**問題**：ベクトル検索で取得した参照 Q&A が質問と無関係な場合、LLM がノイズに引きずられて誤った回答を生成するリスクがあった。

**解決策**：参照選定ステップを独立したノードとして LangGraph に組み込み、LLM に参照の関連度を判定させた上で「参照あり回答」と「参照なし回答」をルーティングで切り替える設計を採用。LangGraph のグラフ構造により分岐ロジックをテスト・可視化しやすくした。

### 課題2：ストリーミングにおける進捗表示

**問題**：RAG パイプラインは複数ステップにわたるため、単純な「ローディング中」表示では体感待ち時間が長く感じられた。

**解決策**：各ノードの完了時に SSE でステータスイベント（8段階）を配信し、フロントエンドでプログレスバーとステップ名を表示。ユーザーが「今どこまで処理が進んでいるか」を把握できるようにした。

### 課題3：多言語での一貫した体験

**問題**：Q&A データに存在しない言語での質問や、言語が混在した質問に対して適切に対応する必要があった。

**解決策**：`langid` による言語検出と、ユーザーが設定した言語プリファレンスを組み合わせてプロンプトの言語を制御。また、翻訳・要約・平易化アクションをチャット後に提供することで、受け取った回答を自分の理解しやすい形に変換できるようにした。

### 課題4：バックエンドの保守性

**問題**：認証・チャット・知識検索など複数のドメインが絡み合い、コードが密結合になりやすい構造だった。

**解決策**：DDD に基づいた5層アーキテクチャを採用し、ドメイン層はフレームワークや DB ドライバに依存しない純粋な Python として実装。`identity / conversation / knowledge / retrieval` の4ドメインを明確に分離した。

---

## 実装上のこだわり

- **型安全性**：FastAPI + Pydantic によるリクエスト/レスポンスの厳密なスキーマ定義
- **ストリーミング UX**：SSE で回答トークンと進捗状態を並行配信し、待ち時間を知覚しにくくする
- **インフラの再現性**：Docker Compose による環境の完全なコード化（IaC）
- **知識源の鮮度管理**：スクレイピング → Embedding 生成 → 再インジェクションの自動化スクリプト

---

## 技術スタック一覧

| レイヤー | 技術 |
|---|---|
| フロントエンド | React 18, React Router, Tailwind CSS, Radix UI |
| バックエンド API | FastAPI, Pydantic, Uvicorn |
| エージェント | LangGraph, LangChain (OpenAI), langid |
| LLM / Embeddings | OpenAI GPT, OpenAI Embeddings API |
| データベース | PostgreSQL 16, pgvector |
| 認証 | JWT (python-jose), bcrypt |
| インフラ | Docker Compose, Nginx |
| 開発ツール | uv (Python パッケージ管理), LangGraph Studio |
